# Personal Homepage (Bento Grid Style)

基于 Vue 3 + TypeScript + Vite 构建的个人主页，采用时尚的 Bento Grid（便当盒）布局与拟态玻璃风格设计。

## ✨ 特性

- **Bento 布局**: 响应式网格系统，适配桌面与移动端。
- **拟态设计**: 精美的玻璃磨砂效果 (Glassmorphism)，支持**深色模式**自动切换。
- **沉浸式音乐播放器**:
  - 集成网易云音乐播放功能。
  - 支持 VIP 歌曲播放 (需配置 Cookie)。
  - 歌词底部悬浮显示，随音乐律动。
  - 播放列表查看与切歌。
- **动态组件**:
  - **ProfileCard**: 个人头像、状态与简介。
  - **TimeCard**: 实时时钟显示。
  - **MottoCard**: 一言/座右铭展示。
  - **LinkBlock**: 社交媒体与项目链接快捷入口。
- **安全防护**: 内置防右键、防 F12 及禁止文本选中功能，保护页面内容。

## 🛠️ 安装与运行

确保你的环境已安装 Node.js (推荐 v16+)。

```bash
# 1. 克隆项目
git clone https://github.com/ERIC-ZHAO-3366/newhome.git
cd homepage

# 2. 安装依赖
npm install

# 3. 本地开发
npm run dev
```

该命令将在 `http://localhost:5173` 启动开发服务器。

## ⚙️ 配置指南

所有的配置均位于 `src/config` 目录下，修改 JSON 文件即可生效。

### 1. 布局与组件配置 (`src/config/grid.json`)
定义卡片的排布、大小及具体参数。
```json
[
  { "id": "profile", "type": "ProfileCard", "size": "2x2" },
  { 
    "id": "music", 
    "type": "MusicCard", 
    "size": "2x1", 
    "props": { 
      "playlistUrl": "https://music.163.com/playlist?id=YOUR_PLAYLIST_ID",
      "apiBase": "你的音乐API服务链接" 
    } 
  },
  ...
]
```

### 2. 个人信息 (`src/config/profile.json`)
修改头像、名称及简介信息。
```json
{
  "name": "EricZhao",
  "bio": "介绍",
  "avatar": "头像",
  "tags": ["标签"]
}
```

### 3. 网易云音乐 VIP 配置 (.env)
若需播放 VIP 歌曲或获取更高音质，请在项目根目录创建或修改 `.env` 文件。
> 注意：`.env` 文件包含敏感信息，请勿提交到公开仓库。

```properties
# .env
VITE_MUSIC_COOKIE="MUSIC_U=xxxxxxxxxxxxxx;"
```
*获取方式：登录网易云官网 -> F12 Network -> 刷新页面 -> 复制请求头中的 `MUSIC_U` Cookie。*

## 📦 打包部署

```bash
# 构建生产环境代码
npm run build
```
构建产物位于 `dist` 目录，可直接上传至 Vercel, Netlify, GitHub Pages 或任何静态服务器。

---
© 2026 EricZhao. All Rights Reserved.
